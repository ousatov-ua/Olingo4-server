<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.olus.olingo4.nnmrls.dao.mappers.Mapper">
    <select id="selectAllEntities" resultType="map" resultSetType="SCROLL_SENSITIVE">
        ${selectStatement}
    </select>

    <update id="updateEntity">
        ${updateStatement}
    </update>

    <update id="insertEntity">
        ${insertStatement}
    </update>

    <select id="selectEntity">
        ${selectStatement}
    </select>
</mapper>